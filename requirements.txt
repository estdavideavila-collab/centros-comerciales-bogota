# Librerias del proyecto. Probado con Python 3.11.
# Se fijan versiones minimas y no exactas: con numpy==2.5.3 la instalacion
# fallaba en Python 3.11, porque esa version exige Python 3.12 o superior.

networkx>=3.4      # estructura del grafo
matplotlib>=3.8    # imagenes estaticas del mapa
geopandas>=1.0     # mapa de localidades
shapely>=2.0       # geometrias (puntos, poligonos)
numpy>=1.25
folium>=0.17       # mapa interactivo (lo genera main.py)
